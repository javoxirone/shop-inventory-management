[tool.ruff]
line-length = 100
target-version = "py313"
exclude = [
  ".venv",
  "alembic/versions",
]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["B008"]

[tool.ruff.lint.per-file-ignores]
"tests/*.py" = ["E402"]
"main.py" = ["F401"]
